# 商戶後台管理系統 (Waffle Backend)

一個基於 Vue 3 的現代化商戶後台管理系統，提供完整的訂單管理、資金結算、數據報表等功能。

## 功能特色

### 📊 數據管理
- **我的帳號**：查看帳戶資訊、餘額、費率設定
- **基本訊息**：管理商戶基本資料、Google 雙因素驗證
- **銀行卡訊息**：管理收款銀行帳戶資訊

### 💰 財務管理
- **訂單紀錄**：查看所有訂單歷史，支援多種支付通道（VNMOMO、VNZALO、VNBANK、QRCODE 等）
- **訂單流水**：即時查看訂單流水明細，支援訂單、內充、結算分類
- **結算紀錄**：管理結算申請與歷史記錄

### 📈 報表分析
- **對帳日報**：生成日報表，包含訂單統計、資金流動分析
- **數據圖表**：使用 ECharts 和 Chart.js 呈現視覺化數據分析

### 🔐 安全功能
- Google 雙因素驗證（2FA）
- 登入密碼與支付密碼管理
- 登入歷史記錄追蹤

### 🌐 多語言支援
- 支援繁體中文、簡體中文、英文、越南文
- 使用 Vue i18n 實現國際化

## 使用技術

### 核心框架
- **Vue.js** `^3.2.13` - 漸進式 JavaScript 框架
- **Vue Router** `^4.0.3` - 官方路由管理器（使用 Hash 模式，適合 GitHub Pages）
- **Vuex** `^4.0.0` - 狀態管理
- **Vue i18n** `^9.2.2` - 國際化插件

### UI 框架與樣式
- **Element Plus** `^2.2.20` - 基於 Vue 3 的組件庫
- **Bootstrap** `^5.2.2` - CSS 框架
- **Font Awesome** `^6.2.0` - 圖標庫
- **Material Icons** `^1.12.1` - Material Design 圖標

### 數據可視化
- **ECharts** `^5.4.0` - 強大的圖表庫
- **Chart.js** `@j-t-mcc/vue3-chartjs ^1.3.0` - Vue 3 的 Chart.js 封裝

### HTTP 請求
- **Axios** `^1.1.3` - HTTP 客戶端
- **Vue Axios** `^3.5.2` - Axios 的 Vue 插件

### 工具庫
- **Moment.js** `^2.29.4` - 日期時間處理庫
- **Lodash** `^4.17.21` - JavaScript 工具庫
- **QRCode.vue** `^3.3.3` - QR Code 生成組件

### 其他功能
- **SweetAlert2** `^11.6.7` - 美觀的彈窗提示
- **Vue Loading Overlay** `^6.0.2` - 載入動畫
- **js-table2excel** `^1.0.3` - 表格導出 Excel

### 開發工具
- **Vue CLI** `~5.0.0` - 專案腳手架
- **ESLint** `^7.32.0` - 代碼檢查工具
- **Sass** `^1.32.7` - CSS 預處理器
- **Babel** `^7.12.16` - JavaScript 編譯器
- **Lint-staged** `^11.1.2` - Git 提交前代碼檢查

## GitHub Pages 線上預覽

🌐 **線上預覽網址：** [https://wafflefrank.github.io/admin_frontEnd/](https://wafflefrank.github.io/admin_frontEnd/)

> 注意：目前使用 Mock API，所有數據均為測試資料

## Project setup

```bash
npm install
```

### Compiles and hot-reloads for development

```bash
npm run serve
```

### Compiles and minifies for production

```bash
npm run build
```

### Lints and fixes files

```bash
npm run lint
```

## GitHub Pages 部署

### 自動部署（推薦）

此專案已配置 GitHub Actions，當您推送代碼到 `main` 或 `master` 分支時，會自動構建並部署到 GitHub Pages。

**設置步驟：**

1. 在 GitHub 倉庫中，進入 **Settings** → **Pages**
2. 在 **Source** 部分，選擇 **GitHub Actions**
3. 推送代碼到 `main` 或 `master` 分支
4. GitHub Actions 會自動構建並部署

部署完成後，您的網站將在以下網址可用：
- `https://<USERNAME>.github.io/<REPO_NAME>/`

### 手動部署

如果需要手動部署，可以使用以下命令：

```bash
npm run build
```

然後將 `dist` 資料夾的內容推送到 `gh-pages` 分支。

### 注意事項

- 此專案使用 Hash 路由模式（`createWebHashHistory`），適合 GitHub Pages
- `publicPath` 已設置為 `'./'`，確保資源路徑正確
- 目前使用 Mock API，所有 API 調用都返回測試資料

## 專案結構

```
admin_frontEnd/
├── src/
│   ├── components/      # 共用組件
│   ├── views/          # 頁面組件
│   ├── router/         # 路由配置
│   ├── i18n/           # 多語言配置
│   ├── mockApi.js      # Mock API 數據
│   └── main.js         # 入口文件
├── public/             # 靜態資源
├── .github/
│   └── workflows/      # GitHub Actions 配置
└── package.json        # 專案依賴
```

## Customize configuration

See [Configuration Reference](https://cli.vuejs.org/config/).
